---
import Base from '../../layouts/Base.astro';
---

<Base title="Diagnostica AI — Studio Legale" description="Check-list diagnostica AI per lo studio legale.">
  <div class="tool-embed">
    <iframe src="/tools/studio-legale.html" title="Diagnostica AI Studio Legale"></iframe>
  </div>
</Base>

<style>
  .tool-embed {
    margin-top: 72px;
  }
  .tool-embed iframe {
    width: 100%;
    min-height: calc(100vh - 72px);
    border: none;
    display: block;
  }
</style>

<script>
  const iframe = document.querySelector('.tool-embed iframe');
  if (iframe) {
    iframe.addEventListener('load', () => {
      try {
        const h = iframe.contentDocument?.documentElement?.scrollHeight;
        if (h) iframe.style.height = h + 'px';
      } catch(e) {}
    });
  }
</script>
